version: "3"
services:
  postgresql:
      container_name: postgresql
      build: postgresql
      restart: always
      environment:
          POSTGRES_PASSWORD: devpass
          POSTGRES_USER: mirrors
      ports:
        - "6543:5432"
  api:
    container_name: api
    build: apache+php
    ports:
      - "420:80"
      - "8000:8000"
    volumes:
        - "../:/app"
    working_dir: /app
    tty: true
    links:
      - postgresql
    depends_on:
      - postgresql